use drybl_mod, only : drybl_get_descriptor
use ideal_squall_mod, only : ideal_squall_get_descriptor
use kidtestcase_mod, only : kidtestcase_get_descriptor
use tank_experiments_mod, only : tank_experiments_get_descriptor
use shallow_convection_mod, only : shallow_convection_get_descriptor
